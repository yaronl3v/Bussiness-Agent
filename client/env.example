# API Configuration
# Note: API URL is automatically detected based on current origin
# - localhost -> http://localhost:3000/api
# - production -> {current-origin}/api
# No environment variables needed for API URL!

# Environment (automatically detected from NODE_ENV)
NODE_ENV=development

# Optional environment variables (if needed for other features)
# VITE_CUSTOM_SETTING=value
